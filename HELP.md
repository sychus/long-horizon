# Long Horizon — Help

Installation and usage now live in **[USAGE.md](USAGE.md)**.

For the technical design and the measurements behind it, see [SPEC.md](SPEC.md).

> This file previously documented an earlier design — a single shared
> `mempalace-data` volume, a fixed port, and an absolute path to the proxy in the
> MCP config. None of that is true any more, so it has been replaced with this
> pointer rather than left to contradict the current docs.
